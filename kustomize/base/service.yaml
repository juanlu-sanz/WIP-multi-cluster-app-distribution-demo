# service.yaml
# Purpose: Exposes the demo-app Deployment within the cluster on port 9090.
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-app
    app.kubernetes.io/part-of: demo-app
  name: demo-app
spec:
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  selector:
    app: demo-app
